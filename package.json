{
  "name": "@example/minimal",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build --experimental-ssr && firebase deploy --only functions,hosting"
  },
  "type": "module",
  "devDependencies": {
    "astro": "^1.0.0-beta.19"
  },
  "dependencies": {
    "@astrojs/preact": "^0.1.2",
    "firebase-functions": "^3.20.1",
    "preact": "^10.7.1"
  }
}
